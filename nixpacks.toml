[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore SalaryManagement.Web/SalaryManagement.Web.csproj"]

[phases.build]
cmds = ["dotnet publish SalaryManagement.Web/SalaryManagement.Web.csproj -c Release -o out"]

[start]
cmd = "dotnet out/SalaryManagement.Web.dll"