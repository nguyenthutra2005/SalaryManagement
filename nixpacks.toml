[phases.setup]
providers = ["csharp"]

[phases.install]
cmds = ["dotnet restore SalaryManagement.Web/SalaryManagement.Web.csproj"]

[phases.build]
cmds = ["dotnet publish SalaryManagement.Web/SalaryManagement.Web.csproj -c Release -o /app/out"]

[start]
cmd = "dotnet /app/out/SalaryManagement.Web.dll"