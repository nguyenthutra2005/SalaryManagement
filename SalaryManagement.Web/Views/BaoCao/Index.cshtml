@model SalaryManagement.Application.DTOs.DashboardDto

@{
    ViewData["Title"] = "Phân Tích Chi Phí";
    var thang = (int)ViewBag.Thang;
    var nam = (int)ViewBag.Nam;
}

@section TopbarActions {
    <form method="get" class="d-flex gap-2 align-items-center">
        <select name="thang" class="form-select-dark">
            @for (int i = 1; i <= 12; i++)
            {
                if (i == thang)
                {
                    @:
                    <option value="@i" selected>Tháng @i</option>
                }
                else
                {
                    @:
                    <option value="@i">Tháng @i</option>
                }
            }
        </select>
        <input type="number" name="nam" value="@nam" class="form-control-dark" style="width:90px" />
        <button class="btn-gold btn">Xem</button>
    </form>
}

<div class="row g-3 mb-4">
    <div class="col-md-3">
        <div class="stat-card">
            <div class="label">Tổng chi phí công ty</div>
            <div class="value" style="color:#f87171">@Model.TongChiPhiCongTy.ToString("N0") ₫</div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="stat-card">
            <div class="label">Tổng lương Gross</div>
            <div class="value" style="color:#60a5fa">@Model.TongLuongGross.ToString("N0") ₫</div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="stat-card">
            <div class="label">Tổng BH công ty đóng</div>
            <div class="value" style="color:#a78bfa">@Model.TongBaoHiemCongTy.ToString("N0") ₫</div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="stat-card">
            <div class="label">Tổng thuế TNCN</div>
            <div class="value" style="color:#f59e0b">@Model.TongThue.ToString("N0") ₫</div>
        </div>
    </div>
</div>

<div class="card-dark">
    <div class="card-header-dark">Chi Tiết Theo Phòng Ban</div>
    <div class="p-3">
        <table class="table-dark-custom">
            <thead>
                <tr>
                    <th>Phòng Ban</th>
                    <th class="text-center">Số NV</th>
                    <th class="text-end">Tổng Gross</th>
                    <th class="text-end">Tổng Net</th>
                    <th class="text-end">Chênh lệch</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.TongHopTheoPhongBan.Any())
                {
                    <tr><td colspan="5" class="text-center" style="color:#6b7280;padding:40px">Chưa có dữ liệu</td></tr>
                }
                @foreach (var pb in Model.TongHopTheoPhongBan)
                {
                    <tr>
                        <td>@pb.TenPhongBan</td>
                        <td class="text-center">@pb.SoLuongNhanVien</td>
                        <td class="text-end">@pb.TongLuongGross.ToString("N0") ₫</td>
                        <td class="text-end" style="color:#34d399">@pb.TongLuongNet.ToString("N0") ₫</td>
                        <td class="text-end" style="color:#f87171">@((pb.TongLuongGross - pb.TongLuongNet).ToString("N0")) ₫</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>